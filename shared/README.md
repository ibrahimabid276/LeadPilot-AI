# Shared Types

This directory contains shared type definitions used across frontend and backend.

## Usage

These types can be imported by both the Next.js frontend and FastAPI backend to ensure consistency.
